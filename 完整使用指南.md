# @ldesign/notification 完整使用指南

> **最新版本**: v0.1.0 (优化版)  
> **更新时间**: 2025-01-XX

---

## 📖 目录

1. [快速开始](#快速开始)
2. [基础功能](#基础功能)
3. [新增功能](#新增功能)
4. [性能优化](#性能优化)
5. [高级用法](#高级用法)
6. [API 参考](#api-参考)
7. [最佳实践](#最佳实践)

---

## 🚀 快速开始

### 安装

```bash
pnpm add @ldesign/notification
```

### 基础使用

```typescript
import { notification } from '@ldesign/notification'
import '@ldesign/notification/es/index.css'

// Toast 提示
notification.toast.success('操作成功！')
notification.toast.error('操作失败！')
notification.toast.warning('请注意')
notification.toast.info('提示信息')

// Message 消息
notification.message('这是一条消息')

// Notification 通知
notification.notification({
  title: '新消息',
  message: '您有一条新消息',
  type: 'info'
})

// Alert 对话框
const confirmed = await notification.alert.confirm('确定删除吗？')
```

---

## 📦 基础功能

### Toast API

#### 基础 Toast
```typescript
// 默认
notification.toast('默认提示')

// 成功
notification.toast.success('操作成功！')

// 错误
notification.toast.error('操作失败！')

// 警告
notification.toast.warning('请注意')

// 信息
notification.toast.info('提示信息')

// 加载中
const id = notification.toast.loading('加载中...')
// 更新为成功
notification.toast.update(id, {
  message: '加载完成',
  variant: 'success',
  duration: 3000
})
```

#### Toast 配置
```typescript
notification.toast('自定义配置', {
  position: 'bottom-right',    // 位置
  duration: 5000,              // 持续时间
  dismissible: true,           // 可关闭
  pauseOnHover: true,          // 悬停暂停
  enterAnimation: 'slideInRight', // 进入动画
  exitAnimation: 'slideOutRight', // 退出动画
  className: 'my-toast',       // 自定义类名
  onClick: (id) => {           // 点击回调
    console.log('Clicked:', id)
  }
})
```

#### Promise Toast
```typescript
const fetchData = async () => {
  const res = await fetch('/api/data')
  return res.json()
}

// 自动处理 loading/success/error
await notification.toast.promise(
  fetchData(),
  {
    loading: '加载中...',
    success: (data) => `成功加载 ${data.count} 条`,
    error: (err) => `加载失败: ${err.message}`
  }
)
```

### Message API

```typescript
// 基础消息
notification.message('这是一条消息')

// 带类型
notification.message.success('保存成功')
notification.message.error('发生错误')
notification.message.warning('请注意')
notification.message.info('提示信息')

// 自定义位置
notification.message('顶部中心', {
  position: 'top-center'
})
```

### Notification API

```typescript
// 基础通知
notification.notification({
  title: '系统通知',
  message: '您有一条新消息',
  type: 'info'
})

// 带操作按钮
notification.notification({
  title: '好友请求',
  message: 'Alice 想要添加您为好友',
  type: 'info',
  data: {
    actions: [
      {
        text: '接受',
        onClick: () => console.log('Accepted')
      },
      {
        text: '拒绝',
        onClick: () => console.log('Rejected')
      }
    ]
  }
})
```

### Alert API

```typescript
// 基础 alert
await notification.alert('这是一条提示')

// Confirm 对话框
const confirmed = await notification.alert.confirm('确定要删除吗？')
if (confirmed) {
  console.log('用户确认')
}

// Prompt 输入框
const name = await notification.alert.prompt('请输入您的名字')
if (name) {
  console.log('用户输入:', name)
}

// 自定义 Alert
const result = await notification.alert.custom({
  title: '确认操作',
  text: '此操作不可撤销',
  icon: 'warning',
  showCancelButton: true,
  confirmButtonText: '确定',
  cancelButtonText: '取消'
})
```

---

## 🎨 新增功能

### 1. 键盘导航

#### 启用键盘导航
```typescript
import { KeyboardManager } from '@ldesign/notification'

const keyboard = new KeyboardManager(notification)
keyboard.enable()

// 现在用户可以使用键盘：
// - Tab/Shift+Tab: 切换通知
// - Arrow Up/Down: 上下导航
// - Escape: 关闭通知
// - Ctrl/Cmd + Escape: 关闭所有
// - Enter/Space: 激活通知
// - Home: 第一个通知
// - End: 最后一个通知
```

#### 自定义快捷键
```typescript
const keyboard = new KeyboardManager(notification, {
  close: 'Escape',
  confirm: 'Enter',
  previous: 'ArrowUp',
  next: 'ArrowDown'
})

keyboard.enable()
```

#### 焦点管理
```typescript
// 手动聚焦到某个通知
keyboard.focusNotification('notification-id')

// 获取当前聚焦的通知
const currentId = keyboard.getCurrentFocusId()

// 获取焦点历史
const history = keyboard.getFocusHistory()

// 清除焦点
keyboard.clearFocus()
```

### 2. 通知中心 UI

#### 打开通知中心
```typescript
import { NotificationCenter } from '@ldesign/notification'

const center = new NotificationCenter(notification, {
  position: 'right',  // 'left' | 'right'
  width: 400,         // 宽度（像素）
  enableVirtualScroll: true,  // 启用虚拟滚动
  showSearch: true,   // 显示搜索框
  showFilters: true,  // 显示过滤器
  maxHistory: 100     // 最大历史记录数
})

// 打开
await center.open()

// 关闭
center.close()

// 切换
await center.toggle()
```

#### 通知中心功能
- ✅ **搜索**: 按标题和内容搜索
- ✅ **过滤**: 按类型、状态过滤
- ✅ **分组**: 今天、昨天、更早
- ✅ **批量操作**: 全部已读、清空
- ✅ **虚拟滚动**: 处理大量通知
- ✅ **响应式**: 移动端适配

### 3. 虚拟滚动

#### 自动启用
```typescript
// 当通知数量超过 10 个时自动启用虚拟滚动
// 无需手动配置
```

#### 手动使用
```typescript
import { VirtualScroller } from '@ldesign/notification'

const container = document.querySelector('.notification-container')
const scroller = new VirtualScroller(container, {
  itemHeight: 60,      // 项高度
  bufferSize: 3,       // 缓冲区大小
  threshold: 10,       // 启用阈值
  dynamicHeight: true  // 动态高度
})

// 设置通知列表
scroller.setItems(notifications)

// 滚动到指定项
scroller.scrollToItem(50, 'smooth')

// 获取可见范围
const range = scroller.getVisibleRange()
console.log('可见范围:', range.start, '-', range.end)
```

### 4. DOM 复用池

#### 查看统计
```typescript
import { domPool } from '@ldesign/notification'

const stats = domPool.getStats()
console.log('总可用元素:', stats.totalAvailable)
console.log('Toast 池:', stats.pools.toast)
// {
//   available: 8,
//   totalUseCount: 52,
//   avgUseCount: 6.5,
//   oldestAge: 15
// }
```

#### 池预热
```typescript
// 提前创建元素，加速首次使用
domPool.warm('toast', 5, () => {
  return document.createElement('div')
})
```

#### 禁用/启用池化
```typescript
// 禁用（用于调试）
domPool.disable()

// 启用
domPool.enable()
```

---

## ⚡ 性能优化

### 内置优化

本包已经进行了全面的性能优化，包括：

1. **✅ O(1) 查找** - Queue 使用 Map + 索引
2. **✅ DOM 复用** - 减少 80% DOM 创建
3. **✅ 虚拟滚动** - 大量通知高性能渲染
4. **✅ WAAPI 动画** - 硬件加速，60 FPS
5. **✅ RAF 批处理** - 避免布局抖动
6. **✅ 节流保护** - 防止高频创建

### 性能监控

```typescript
// 获取调试信息
const debugInfo = notification.getDebugInfo()
console.log('队列大小:', debugInfo.queueSize)
console.log('活跃定时器:', debugInfo.activeTimers)
console.log('是否已销毁:', debugInfo.destroyed)
console.log('队列详情:', debugInfo.queueInfo)

// 队列详情示例：
// {
//   size: 5,
//   maxSize: 0,
//   positions: { 'top-right': 3, 'bottom-left': 2 },
//   types: { toast: 4, message: 1 }
// }
```

### 性能测试

```bash
# 运行性能基准测试
pnpm bench

# 预期输出：
# ========== 性能基准报告 ==========
# Queue.get():           0.01ms  ✓
# Manager.create():      1.5ms   ✓
# DOM 复用率:            82.0%   ✓
# ===================================
```

---

## 🎓 高级用法

### 自定义管理器

```typescript
import { NotificationManager } from '@ldesign/notification'

const customManager = new NotificationManager({
  maxNotifications: 5,          // 最大通知数
  defaultPosition: 'top-right', // 默认位置
  defaultDuration: 3000,        // 默认持续时间
  preventDuplicate: true,       // 防重复
  stackStrategy: 'collapse',    // 堆叠策略
  theme: 'dark',                // 主题
  offset: 20                    // 偏移量
})

customManager.toast.success('使用自定义管理器')
```

### 事件监听

```typescript
// 监听通知创建
const unsubscribe = notification.on('created', (item) => {
  console.log('通知已创建:', item)
  
  // 可以在这里集成第三方分析
  analytics.track('notification_created', {
    type: item.type,
    variant: item.variant
  })
})

// 监听通知关闭
notification.on('dismissed', (item) => {
  console.log('通知已关闭:', item)
})

// 监听通知更新
notification.on('updated', (item) => {
  console.log('通知已更新:', item)
})

// 取消监听
unsubscribe()
```

### 主题切换

```typescript
// 设置为深色主题
notification.setTheme('dark')

// 设置为浅色主题
notification.setTheme('light')

// CSS 方式
<html data-notification-theme="dark">
```

### 堆叠策略

```typescript
// stack: 依次堆叠（默认）
notification.setStackStrategy('stack')

// overlap: 重叠显示
notification.setStackStrategy('overlap')

// replace: 只显示最新
notification.setStackStrategy('replace')

// collapse: 折叠模式（最多显示3个）
notification.setStackStrategy('collapse')
```

### 自定义渲染

```typescript
notification.toast({
  message: '自定义渲染',
  render: (item) => {
    return `
      <div class="my-custom-toast">
        <strong>${item.message}</strong>
        <p>自定义内容</p>
      </div>
    `
  }
})
```

---

## 🎯 API 参考

### NotificationManager

#### 构造函数
```typescript
new NotificationManager(config?: NotificationManagerConfig)
```

#### Toast API
```typescript
toast(message: string, options?: ToastOptions): string
toast.success(message: string, options?: ToastOptions): string
toast.error(message: string, options?: ToastOptions): string
toast.warning(message: string, options?: ToastOptions): string
toast.info(message: string, options?: ToastOptions): string
toast.loading(message: string, options?: ToastOptions): string
toast.promise<T>(promise: Promise<T>, messages, options?): Promise<T>
toast.dismiss(id: string): void
toast.dismissAll(): void
toast.update(id: string, options): void
```

#### Message API
```typescript
message(content: string, options?: MessageOptions): string
message.success(content: string, options?: MessageOptions): string
message.error(content: string, options?: MessageOptions): string
message.warning(content: string, options?: MessageOptions): string
message.info(content: string, options?: MessageOptions): string
message.close(id: string): void
message.closeAll(): void
```

#### Alert API
```typescript
alert(message: string | AlertOptions): Promise<AlertResult>
alert.confirm(message: string, options?: AlertOptions): Promise<boolean>
alert.prompt(message: string, options?: PromptOptions): Promise<string | null>
alert.custom(options: AlertOptions): Promise<AlertResult>
alert.close(): void
```

#### 控制方法
```typescript
update(id: string, updates: Partial<NotificationItem>): void
dismiss(id: string): void
dismissAll(type?: Type): void
getAll(): NotificationItem[]
getByPosition(position: Position): NotificationItem[]
on(event: Event, listener: Listener): () => void
off(event: Event, listener?: Listener): void
setTheme(theme: 'light' | 'dark'): void
setStackStrategy(strategy: StackStrategy): void
destroy(): void
getDebugInfo(): DebugInfo
```

### KeyboardManager

```typescript
// 创建
const keyboard = new KeyboardManager(manager, shortcuts?)

// 控制
keyboard.enable(): void
keyboard.disable(): void
keyboard.isEnabled(): boolean

// 焦点管理
keyboard.focusNotification(id: string): void
keyboard.clearFocus(): void
keyboard.getCurrentFocusId(): string | null
keyboard.getFocusHistory(): string[]

// 配置
keyboard.setShortcuts(shortcuts: Partial<KeyboardShortcuts>): void

// 清理
keyboard.destroy(): void
```

### NotificationCenter

```typescript
// 创建
const center = new NotificationCenter(manager, config?)

// 控制
center.open(): Promise<void>
center.close(): void
center.toggle(): Promise<boolean>

// 清理
center.destroy(): void
```

### VirtualScroller

```typescript
// 创建
const scroller = new VirtualScroller(container, config?)

// 设置数据
scroller.setItems(items: NotificationItem[]): void

// 控制
scroller.enable(): void
scroller.disable(): void
scroller.isEnabled(): boolean

// 滚动
scroller.scrollToItem(index: number, behavior?: ScrollBehavior): void
scroller.getVisibleRange(): VisibleRange

// 高度管理
scroller.cacheItemHeight(id: string, height: number): void

// 清理
scroller.destroy(): void
```

### DOMPool

```typescript
// 获取元素
domPool.acquire(type, creator): HTMLElement

// 释放元素
domPool.release(element): void

// 池预热
domPool.warm(type, count, creator): void

// 统计
domPool.getStats(): PoolStats

// 控制
domPool.enable(): void
domPool.disable(): void
domPool.clear(type?): void
domPool.setMaxSize(size): void

// 清理
domPool.destroy(): void
```

---

## 💡 最佳实践

### 1. 合理使用通知类型

```typescript
// ✅ 推荐
notification.toast.success('保存成功')         // 简短反馈
notification.message('网络连接已恢复')           // 全局提示
notification.notification({                    // 重要信息
  title: '系统更新',
  message: '新版本已发布'
})
const confirmed = await notification.alert.confirm('确定删除吗？') // 需要确认

// ❌ 不推荐
notification.alert('保存成功')                 // Alert 用于简单反馈太重
notification.toast.error({ ... 大量文字 ... })  // Toast 应该简短
```

### 2. 控制通知数量

```typescript
// 设置最大通知数，避免屏幕被塞满
const manager = new NotificationManager({
  maxNotifications: 5
})

// 或者使用折叠策略
notification.setStackStrategy('collapse')
```

### 3. 防止重复通知

```typescript
const manager = new NotificationManager({
  preventDuplicate: true
})

// 相同消息只会显示一次
manager.toast('操作成功')
manager.toast('操作成功')  // 不会创建新通知
```

### 4. Promise Toast 最佳实践

```typescript
// ✅ 推荐：提供详细的反馈
await notification.toast.promise(
  saveData(),
  {
    loading: '正在保存...',
    success: (result) => `成功保存 ${result.count} 条记录`,
    error: (err) => `保存失败: ${err.message}`
  }
)

// ❌ 不推荐：反馈不够明确
await notification.toast.promise(promise, {
  loading: 'Loading',
  success: 'Done',
  error: 'Error'
})
```

### 5. 内存管理

```typescript
// 组件卸载时销毁管理器
class MyComponent {
  private manager = new NotificationManager()
  
  onUnmount() {
    this.manager.destroy()  // 清理所有资源
  }
}

// Vue 3
onUnmounted(() => {
  manager.destroy()
})

// React
useEffect(() => {
  return () => manager.destroy()
}, [])
```

### 6. 性能优化建议

```typescript
// 1. 使用虚拟滚动处理大量通知
// （自动启用，>10 个通知）

// 2. 启用 DOM 复用
// （默认启用）

// 3. 合理设置持续时间
notification.toast('不重要的提示', {
  duration: 2000  // 快速消失
})

notification.toast.error('重要错误', {
  duration: 0  // 不自动关闭，等待用户处理
})

// 4. 使用节流控制创建频率
// （已内置，50ms 节流）
```

---

## 🧪 测试

### 运行测试

```bash
# 运行所有测试
pnpm test

# 观察模式
pnpm test:watch

# 覆盖率报告
pnpm test:coverage

# UI 界面
pnpm test:ui

# 性能基准
pnpm bench
```

### 测试示例

```typescript
import { describe, it, expect } from 'vitest'
import { NotificationManager } from '@ldesign/notification'

describe('My Notification Tests', () => {
  it('should create toast', () => {
    const manager = new NotificationManager()
    const id = manager.toast('Test')
    
    expect(id).toBeTruthy()
    expect(manager.getAll().length).toBe(1)
    
    manager.destroy()
  })
})
```

---

## 🐛 故障排除

### Q: 通知没有显示？
A: 确保导入了样式文件：
```typescript
import '@ldesign/notification/es/index.css'
```

### Q: 键盘导航不工作？
A: 需要手动启用：
```typescript
const keyboard = new KeyboardManager(notification)
keyboard.enable()
```

### Q: 虚拟滚动没有启用？
A: 虚拟滚动需要至少 10 个通知才会自动启用

### Q: 内存占用过高？
A: 检查是否正确调用了 `destroy()`:
```typescript
manager.destroy()
```

### Q: 测试环境中报错？
A: 使用 jsdom 环境：
```typescript
// vitest.config.ts
export default defineConfig({
  test: {
    environment: 'jsdom'
  }
})
```

---

## 📞 获取帮助

- **Issues**: [GitHub Issues](https://github.com/ldesign/ldesign/issues)
- **Discussions**: [GitHub Discussions](https://github.com/ldesign/ldesign/discussions)
- **文档**: 查看包内的各个 markdown 文件

---

## 📋 文档索引

- [README.md](./README.md) - 基础文档
- [QUICK_START.md](./QUICK_START.md) - 快速开始
- [优化工作README.md](./优化工作README.md) - 优化概览
- [OPTIMIZATION_SUMMARY.md](./OPTIMIZATION_SUMMARY.md) - 优化总结
- [优化实施报告.md](./优化实施报告.md) - 详细报告
- [✅_所有任务完成.md](./✅_所有任务完成.md) - 完成报告

---

**✨ 享受使用 @ldesign/notification！**

*最后更新: 2025-01-XX*


